registry=https://npm-proxy.cloud.databricks.com/
# react-simple-maps@3 declares a stale React <=18 peer range but runs fine on
# React 19. Allow the install rather than pinning React back.
legacy-peer-deps=true
