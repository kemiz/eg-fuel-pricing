# ---------------------------------------------------------------------------
# EG Fuel Price Optimisation — local dev env
# ---------------------------------------------------------------------------
# Copy to `.env.local` and fill in. Two ways to reach Lakebase:
#
#   Mode A (recommended): use your Databricks CLI profile; the SDK mints OAuth
#   tokens per connection.
#     EG_DATABRICKS_HOST=https://fe-vm-alice.cloud.databricks.com
#     DATABRICKS_PROFILE=alice
#     EG_LAKEBASE_ENDPOINT=projects/eg-fuel-pricing/branches/production/endpoints/primary
#     EG_LAKEBASE_DB=eg_fuel_pricing
#
#   Mode B: a static Postgres connection string (no token rotation). Handy for
#   pointing at a local Postgres while building the UI.
#     EG_LAKEBASE_URL=postgresql://user:pass@localhost:5432/eg_fuel_pricing

EG_DATABRICKS_HOST=https://fe-vm-alice.cloud.databricks.com
DATABRICKS_PROFILE=alice

EG_LAKEBASE_ENDPOINT=projects/eg-fuel-pricing/branches/production/endpoints/primary
EG_LAKEBASE_DB=eg_fuel_pricing
EG_PG_APP_SCHEMA=eg_app

# Mode B alternative (uncomment to use a static URL instead of SDK OAuth):
# EG_LAKEBASE_URL=postgresql://postgres:postgres@localhost:5432/eg_fuel_pricing

# Model Serving endpoints (foundation model APIs)
EG_LLM_ENDPOINT=serving-endpoints/databricks-claude-sonnet-4-6/invocations
EG_LLM_FAST_ENDPOINT=serving-endpoints/databricks-claude-haiku-4-5/invocations

# Auth for Model Serving + warehouse (local dev). Prefer OAuth client creds in
# deployment; locally a PAT is simplest:
# DATABRICKS_TOKEN=dapi...
